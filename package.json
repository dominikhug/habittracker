{
  "name": "habittracker",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "npm cache clean --force && cd frontend && npm ci && npm run build && cd ../backend && npm ci && npm run build",
    "start": "node backend/dist/server.js"
  }
}
